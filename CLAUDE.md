# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

This repository is uninitialized. The only tracked file is `LICENSE` (MIT, © 2026
Maciej Kosiedowski) — there is currently no source code, dependency manifest, build
tooling, tests, or CI configuration. There is also no README or editor-rule files
(`.cursorrules`, `.cursor/rules/`, `.github/copilot-instructions.md`) to draw guidance from.

As a result there are no build, lint, test, or run commands to document yet.

## When the project is scaffolded

Once code is added, replace this section with:

- **Commands** — how to build, lint, run, and test, including how to run a single test.
- **Architecture** — the big-picture structure that spans multiple files and isn't
  obvious from reading any single one.

The repository name suggests it may relate to the [IndexNow](https://www.indexnow.org/)
protocol (notifying search engines of URL changes), but nothing in the code confirms
this yet — verify the actual purpose before documenting it.

## Repository

- GitHub: `maciej-kosiedowski/indexnow`
- License: MIT
